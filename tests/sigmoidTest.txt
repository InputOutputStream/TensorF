// template<typename T>
// vector<T> sigmoid(vector<T> v)
// {
//     vector<T> arr;
//     for(auto i=0; i<(int)b.size(); i++)
//     { 
//         T prod = 1 /(1+ exp(-1 * v[i]));
//         arr.push_back(prod);
//     }

//     return arr;
// }

void sigmoidTest()
{
    Tensor<float> w0(vector<float> {2});
    Tensor<float> x0(vector<float> {-1});

    cout<<"code 1\n";
    Tensor<float> w1(vector<float> {-3});
    Tensor<float> x1(vector<float> {-2});
    
    cout<<"code 2\n";
    Tensor<float> bias(vector<float> {-3});



    Tensor<float> neg = Tensor<float>(vector<float>{-1});
    Tensor<float> one = Tensor<float>(vector<float>{1});
    cout<<"code 3\n";

    Tensor<float> b = (w0*x0 + w1*x1 + bias) * neg;
    cout<<"code 4\n";

    Tensor<float> c = one / (one + b.exp());
    cout<<"code 5...................\n";
 
    c.backward(vector<float>{1});

    cout<<"code 6\n";

    printTensor(w0.grad);
    printTensor(x0.grad);
    
    printTensor(w1.grad);
    printTensor(x1.grad);

    printTensor(bias.grad);
    
}






    //sigmoidTest();

    //Matrix<int> m1({{1,2}}, {2, 1});

    //Matrix<int> m2({1,2}, {1, 2});
  
    //auto m = m1.dot(m2);

    //cout <<m; // attendu 5
     
    //printTensor(s.val);

