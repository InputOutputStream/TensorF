
     Tensor<float> one(vector<float> {8, 8});
     Tensor<float> two(vector<float> {2, 2});
     Tensor<float> n = two.exp();
     Tensor<float> three = (one) + (two); // l'addition passe creme pour les tenseurs 
     Tensor<float> four(vector<float> {7, 45});

     Tensor<float> five = three - four; // la soustraction passe creme pour les tenseurs 
     five.backward(vector<float> {1, 4}); // l'backward passe creme pour les tenseurs 

     three.backward(vector<float> {1, 5});// l'backward passe creme pour les tenseurs 

     cout <<"Prints.................................."<<endl;
     printTensor(n.val);
          cout <<".................................."<<endl;
     printTensor(four.grad);
          cout <<".................................."<<endl;
     printTensor(three.grad);
     cout <<".................................."<<endl;
     printTensor(two.grad);
         cout <<".................................."<<endl;
     printTensor(one.grad);

     cout <<".................................."<<endl;

    cout<<"Most of the ops passe freely for tensors\n";

    /*
 //sigmoidTest();

*/
