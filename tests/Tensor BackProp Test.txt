   Tensor<float> w0(vector<float> {2});
    Tensor<float> x0(vector<float> {-1});

    Tensor<float> w1(vector<float> {-3});
    Tensor<float> x1(vector<float> {-2});
    
    Tensor<float> bias(vector<float> {-3});



    Tensor<float> neg(vector<float>{-1});
    Tensor<float> one(vector<float>{1});

    Tensor<float> b = neg*(w0*x0 + w1*x1 + bias) ;

    //On a un probleme avec la backprop de division operation
    //On a un probleme avec la backprop de exponent operation lorsqu'elle a ete associe a une operation

    Tensor<float> k = b.exp();
    Tensor<float> c = k*one;

    cout<<"Avant backward..........................................................\n";
    c.backward(vector<float>{1});
    cout<<"Apres backward..........................................................\n";

    printTensor(w0.grad);
    printTensor(x0.grad);
    
    printTensor(w1.grad);
    printTensor(x1.grad);

    printTensor(bias.grad);
   
    return 0;